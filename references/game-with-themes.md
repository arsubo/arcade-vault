# Juegos con skins

Lo mantiene el subagente `skin-designer` (`.claude/agents/skin-designer.md`). Un juego entra en
la tabla solo cuando sus 3 skins pasan `npm run check:skins <game-id>` y `npm run build`. Trabaja
un juego por corrida — nunca asumas que "sin skins todavía" significa que faltan todos a la vez si
la corrida se detuvo a mitad de camino; confía en las sondas de la Fase 0 del agente, no en esta
lista, para saber qué falta técnicamente.

| ID           | Carpeta     | Skins                      | Fecha      | Notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------ | ----------- | -------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `asteroides` | `asteroids` | `clásico`, `neon`, `retro` | 2026-08-05 | Primera corrida: además del juego se levantó toda la fundación (Fases 1, 2 y 4). Decisiones no obvias: (1) `particle` viaja como tripleta `[r,g,b]` y no como string, porque su alpha es dinámico. (2) Las 5 clases del motor (`Bullet`, `Asteroid`, `PowerUp`, `Ship`, `Particle`) están a nivel de módulo: reciben `pal` como argumento en cada `draw`, no por clausura. (3) `setSkin` llama a `draw()` directo — el loop hace `return` antes de dibujar si está en pausa. (4) **Sin reglas `sibling`**: el Asteroides original es monocromo (nave, roca y bala son las tres `"#fff"`), así que exigir que se distingan entre sí haría fallar a `clasico`, que es intocable. (5) `useSkin` usa `useSyncExternalStore` en vez de `useState`+`useEffect`: la regla `react-hooks/set-state-in-effect` del repo prohíbe la variante con efecto, y `getServerSnapshot` resuelve el mismatch de hidratación igual de bien.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tetris`     | `tetris`    | `clásico`, `neon`, `retro` | 2026-08-05 | Corrida en paralelo con arkanoid y snake, cada una en su propio worktree, luego integradas a mano sobre esta rama. Decisiones no obvias: (1) `pieces` es un array de 8 (no campos con nombre): el motor indexa piezas 1..8 y `pieces[n-1]`, así que las reglas de contraste usan tokens con path (`"pieces.0"`) resueltos por `resolveToken` en el verificador. (2) `bg` no tenía literal propio — el canvas es transparente y hereda el fondo de `.crt-screen` — así que su `clasico.bg` reutiliza el `#000` que ya era su fondo real, no un valor inventado. (3) El bisel superior de cada bloque se verifica con una clase nueva `veil` (solo techo, ≤2.20:1, sin piso): un bisel es sutil por diseño y no transporta información, el riesgo es que un alpha alto lo aplane sobre el color de la pieza. (4) La distinción entre piezas vecinas (`sibling`) solo se exige en `retro` — en `clasico`/`neon` las piezas se distinguen por matiz, y forzar luminancia ahí rompería la paleta clásica (8 elementos a ≥3:1 contra el fondo dejan <7x de rango, y 7 saltos de 1.5:1 piden ~17x). Por eso `ContrastRule` ganó un campo opcional `skins?: SkinId[]` para acotar una regla a skins puntuales.                                                                                                                       |
| `arkanoid`   | `arkanoid`  | `clásico`, `neon`, `retro` | 2026-08-05 | Corrida en paralelo con tetris y snake (ver nota de tetris). Decisiones no obvias: (1) **Atlas teñido, no colores planos**: `clip` + `source-atop` + `globalAlpha = 0.85` sobre una copia del PNG, una hoja por skin cacheada a nivel de módulo; `clasico` no tiñe nada y usa el atlas original. (2) **Dedupe por coordenada**: `EXPLOSION_FRAMES.gray` apunta a los mismos `sx,sy` que `.red`; sin dedupe se encadenaban dos tintes — gana el primero (rojo). (3) `clasico` tiene los tokens de sprite (`paddle`, `ball`, `brick*`) en `null`: el Arkanoid original no tenía un solo literal de color para esas regiones, son puro atlas. El verificador trata `null` como "esta skin no pinta esto", no como infractor. (4) `retro`: la paleta cede el brillo a la pelota (`paddle: scale[3]`, no `accent`) — con `accent` la pelota quedaba a 1.47:1 contra la paleta; así queda en 2.67:1. (5) Botones del overlay de pausa verificados por los dos lados (relleno y borde) y en los dos estados (activo/inactivo); el relleno inactivo va como `sibling` (su afordancia la carga el borde) porque el `"#444"` de `clasico` da 2.16:1 contra el velo, insuficiente para `play`. (6) Sin reglas `sibling` entre ladrillos: su color no significa nada mecánicamente y sería insatisfacible (`retro` es de un solo matiz). |
| `snake`      | `snake`     | `clásico`, `neon`, `retro` | 2026-08-05 | Corrida en paralelo con tetris y arkanoid (ver nota de tetris). Decisiones no obvias: (1) El atlas de frutas (22 regiones, fotográficas y multi-tono) **no se tiñe**: se dibuja un "plato" de color (`fruitPlate`) detrás del sprite, que queda intacto encima; en `clasico` el plato es `null` porque la fruta nunca lo tuvo. (2) Cabeza vs. cuerpo lleva regla `sibling` (deben distinguirse de un vistazo), y también `fruitPlate` vs. cuerpo/cabeza — este último par forzó a subir `SKIN_RAMP.neon.danger` a un tono que sí despega de `scale[2]`/`scale[6]` (se corrigió en el slot compartido, nunca con un literal propio del juego).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## Nota sobre el verificador (vale para las próximas corridas)

`scripts/check-skin-contrast.mjs` **no** barre los 8 escalones de una `scale` entre sí con la
clase `sibling`: es matemáticamente incompatible con `play`. Si los 8 tienen que dar ≥ 3.0:1
contra el fondo, su luminancia mínima queda fijada y el rango total disponible cae a ~6.7x,
mientras que 7 saltos consecutivos de 1.5:1 exigen 17x. La clase `sibling` se aplica solo a los
pares que el jugador compara lado a lado, y esos se declaran a mano en `GAME_CONTRAST_RULES`
(`lib/skins.ts`) por juego — así se resolvieron para Tetris (piezas vecinas, solo en
`retro`) y Snake (cabeza vs. cuerpo, plato de fruta vs. ambos).

## Sin skins todavía

Ninguno: los 4 juegos del catálogo (`asteroides`, `tetris`, `arkanoid`, `snake`) tienen
sus 3 skins.
